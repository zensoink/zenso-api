$kind: http-example
request:
  url: "{{baseUrl}}/devices/:uid/display?format=string"
  method: GET
  queryParams:
    format: string
  pathVariables:
    uid: string
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
