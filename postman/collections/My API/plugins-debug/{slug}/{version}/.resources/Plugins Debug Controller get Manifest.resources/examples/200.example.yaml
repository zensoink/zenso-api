$kind: http-example
request:
  url: "{{baseUrl}}/plugins-debug/:slug/:version"
  method: GET
  pathVariables:
    slug: string
    version: string
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
